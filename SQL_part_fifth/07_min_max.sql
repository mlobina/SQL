-- SELECT MAX(num_seats) FROM tables;

-- SELECT MAX(amount_billed) AS max_billed, 
-- MAX(amount_tipped) AS max_tipped FROM bookings;

-- SELECT MAX(category), MIN(category) FROM tables;

SELECT MAX(booking_date), MIN(booking_date) FROM bookings;


