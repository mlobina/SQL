CREATE TABLE payment_methods (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200)
);

CREATE TABLE tables (
    id INT PRIMARY KEY AUTO_INCREMENT,
    num_seats INT,
    category VARCHAR(200)
);INSERT INTO payment_methods (id, name)
VALUES (id:intINSERT INTO tables (id, num_seats, category)
VALUES (
    id:int,
    num_seats:int,
    'category:varchar'
  );, 'name:varchar');